{
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "npm test",
  "start": "sudo service docker start && sleep 2",
  "terminals": [
    {
      "name": "LuckCoin",
      "command": "docker compose up --build",
      "ports": [3000]
    }
  ]
}
